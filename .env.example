# Dataverse CLI Configuration
# Copy this file to .env and fill in your actual values

# ============================================
# SERVICE PRINCIPAL AUTHENTICATION (Recommended for CLI)
# ============================================
# This is the most secure method for CLI automation
# Requires Azure AD app registration with service principal

DATAVERSE_URL=https://your-org.crm.dynamics.com
DATAVERSE_CLIENT_ID=your-azure-app-client-id
DATAVERSE_CLIENT_SECRET=your-azure-app-client-secret
DATAVERSE_TENANT_ID=your-azure-tenant-id

# Optional: Specify environment ID if needed
# DATAVERSE_ENVIRONMENT_ID=your-environment-id


# ============================================
# USER AUTHENTICATION (Alternative)
# ============================================
# Use username/password for user-delegated permissions
# Less secure for CLI, but useful for testing

# DATAVERSE_URL=https://your-org.crm.dynamics.com
# DATAVERSE_CLIENT_ID=your-azure-app-client-id
# DATAVERSE_TENANT_ID=your-azure-tenant-id
# DATAVERSE_USERNAME=your-username@domain.com
# DATAVERSE_PASSWORD=your-password


# ============================================
# ACCESS TOKEN AUTHENTICATION (For testing)
# ============================================
# Use if you already have an access token
# Token will expire after 1 hour typically

# DATAVERSE_URL=https://your-org.crm.dynamics.com
# DATAVERSE_ACCESS_TOKEN=your-access-token


# ============================================
# NOTES
# ============================================
# 1. Never commit the .env file to version control
# 2. Keep your client secret secure
# 3. Rotate credentials regularly
# 4. Dataverse CLI supports service principal for production/automation
# 5. The Dataverse CLI will try authentication methods in this order:
#    - Access Token (if provided)
#    - Service Principal (if configured)
#    - User Credentials (if configured)
